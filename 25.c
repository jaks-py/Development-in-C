/* Código com variáveis do tipo float (ponto flutuante) */

#include <stdio.h>
int main(){
float a = 1000.43;
float b = 1000.0;
float resultado;
resultado = a-b;
printf("O valor de  A-B e %f\n", resultado);
return (0);
}